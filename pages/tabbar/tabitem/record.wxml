<view class="tabitem">
    <image style="width:{{item.image_width}};height:{{item.image_height}}" src="{{item.image}}"></image>
    <text class="text" style="height:{{item.text_height}};color:{{item.text_color}}">{{item.name}}</text>
</view>