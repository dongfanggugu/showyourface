<view class="global">
  <view id="mainpage" class="loop"></view>
  <view class="showinfo"></view>
  <view class="operation"></view>
  <view id="{{itemName.id}}" class="list" wx:for="{{array}}" wx:for-index="index"  wx:for-item="itemName">
        <text>{{index}}</text>
        <text>{{itemName.unique}}</text>
    </view>

</view>